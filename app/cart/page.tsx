import { CartPageContent } from "@/components/alt/cart-page-content";

export const metadata = {
  title: "Cart | Avero Loose Theory"
};

export default function CartPage() {
  return <CartPageContent />;
}
